%h2= t("feedback.step_2")

.alert.alert-success
  As you selected `success`, please give us Your name, etc...

= simple_form_for @feedback, html: { class: "form-vertical" } do |f|
  = f.input :name, input_html: { class: "span6" }, label: t("feedback.name")
  = f.input :website_url, input_html: { class: "span6" }, label: t("feedback.website_url")
  = f.input :image, as: :file, label: t("feedback.image")

  .form-group
    .form-actions
      = f.submit "Submit feedback", class: "btn btn-default btn-primary"
      = link_to t("feedback.skip"), "http://codeguard.com", class: "btn"
      .muted= t("feedback.submit_information_notice")